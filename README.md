# visitor-counter-azure-func

## Running

1. `npm install`
2. `npm start`
3. `func start`

## Setup

1. `func init . --typescript`
2. `func new --name HttpExample --template "HTTP trigger" --authlevel "anonymous"`

